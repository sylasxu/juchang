// apps/api/src/schedules/index.ts
// import { setupCleanupSchedule } from './cleanup.schedule';
// import { setupAnchorSchedule } from './anchor.schedule';

export const initSchedules = () => {
  console.log('🕒 初始化定时任务...');
  
        // setupCleanupSchedule();
        // setupAnchorSchedule();
  
  console.log('✅ 定时任务已就绪');
};