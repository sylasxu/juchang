# 聚场小程序

聚场 AI碎片化社交找搭子平台的微信小程序端，基于 TDesign 组件库构建。

## 功能特性

- 🎯 **AI 智能匹配**：基于用户兴趣和位置的智能搭子推荐
- 📍 **LBS 定位服务**：附近的人和活动发现
- 💬 **实时聊天**：即时通讯功能
- 🎉 **活动发布**：创建和参与各类社交活动
- 👤 **个人中心**：完整的用户资料管理
- 🔐 **微信登录**：一键登录，安全便捷


## 开发指南

### 环境要求

- 微信开发者工具
- Node.js >= 18
- Bun >= 1.3.4

### 快速开始

1. **在项目根目录启动完整开发环境**：
   ```bash
   bun run dev:full
   ```

2. **打开微信开发者工具**，导入 `apps/miniprogram` 目录

3. **构建 npm 包**：在微信开发者工具中点击 "工具" -> "构建 npm"

### API 集成

小程序使用自动生成的类型安全 API：

```typescript
import { postAuthWxLogin, getUsersMe } from '@/api'

// 微信登录
const response = await postAuthWxLogin({ code: 'wx_code' })

// 获取用户信息  
const user = await getUsersMe()
```

API 代码会在后端变更时自动重新生成，确保类型安全。

### 目录结构

```
apps/miniprogram/
├── src/
│   ├── api/          # 自动生成的 API 代码
│   ├── components/   # 通用组件
│   ├── pages/        # 页面文件
│   ├── stores/       # 状态管理 (Zustand)
│   ├── utils/        # 工具函数
│   └── types/        # 类型定义
├── app.ts           # 小程序入口
└── project.config.json
```

### 基础库版本

最低基础库版本 `^2.6.5`

## 技术栈

- **UI 组件**：TDesign MiniProgram
- **状态管理**：Zustand + Immer
- **API 调用**：Orval 生成的类型安全客户端
- **开发语言**：TypeScript
- **构建工具**：微信开发者工具

## 许可证

MIT License - 详见根目录 [LICENSE](../../LICENSE) 文件
