<!--
  创建草稿卡片组件
  Requirements: 3.5, 3.7
  
  显示 AI 解析的活动草稿预览
  点击"立即发布"跳转创建页并预填数据
-->
<view class="draft-card" bindtap="onCardTap">
  <!-- 头部：图标 + 类型标签 -->
  <view class="draft-header">
    <view class="draft-icon">{{typeIcon}}</view>
    <view class="draft-type">{{typeLabel}}</view>
  </view>
  
  <!-- 标题 -->
  <view class="draft-title">{{draft.title}}</view>
  
  <!-- 信息列表 -->
  <view class="draft-info">
    <!-- 时间 -->
    <view class="info-item" wx:if="{{draft.startAt}}">
      <t-icon name="time" size="32rpx" class="info-icon" />
      <text>{{draft.startAt}}</text>
    </view>
    
    <!-- 地点 -->
    <view class="info-item" wx:if="{{draft.location.name}}">
      <t-icon name="location" size="32rpx" class="info-icon" />
      <text>{{draft.location.name}}</text>
    </view>
    
    <!-- 人数 -->
    <view class="info-item" wx:if="{{draft.maxParticipants}}">
      <t-icon name="user" size="32rpx" class="info-icon" />
      <text>{{draft.maxParticipants}}人</text>
    </view>
  </view>
  
  <!-- 发布按钮 -->
  <view class="draft-footer">
    <view class="publish-btn" catchtap="onPublishTap">
      <text class="publish-icon">🚀</text>
      <text>立即发布</text>
    </view>
  </view>
</view>
