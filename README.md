# 聚场 (JuChang)

AI 碎片化社交找搭子平台 - Agent-First + Chat-First + Generative UI

## 🎯 产品定位

聚场是一个 **Personal Social Agent**，不是传统工具。首页即对话，所有功能封装在 Widget 气泡中。

## 🏗️ 技术架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Client Layer                                    │
├─────────────────────────────────┬───────────────────────────────────────────┤
│     📱 微信小程序 (Chat-First)   │           🖥️ Admin 后台                    │
│  ┌───────────────────────────┐  │  ┌─────────────────────────────────────┐  │
│  │ Native WXML + TS + LESS   │  │  │ Vite + React 19 + TanStack Router  │  │
│  │ Zustand Vanilla (~2KB)    │  │  │ Eden Treaty (类型安全 RPC)          │  │
│  │ Orval SDK (自动生成)       │  │  │ AI Playground (执行追踪)            │  │
│  └───────────────────────────┘  │  └─────────────────────────────────────┘  │
└─────────────────────────────────┴───────────────────────────────────────────┘
                                    │
                              SSE / REST
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           API Gateway (ElysiaJS + Bun)                       │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌───────────┐ │
│  │  Auth   │ │  Users  │ │Activities│ │  Chat   │ │Dashboard│ │Notifications│
│  │ 微信登录 │ │ 用户管理 │ │ 活动CRUD │ │ 群聊消息 │ │ 数据聚合 │ │  通知管理  │ │
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘ └───────────┘ │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐   │
│  │                        🤖 AI Module (核心)                            │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │   │
│  │  │  Agent   │ │   RAG    │ │  Intent  │ │  Memory  │ │  Tools   │   │   │
│  │  │ 封装层   │ │ 语义检索  │ │ 意图分类  │ │ 记忆系统  │ │ 工具注册  │   │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘   │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐   │   │
│  │  │  Models  │ │ Prompts  │ │ Workflow │ │Guardrails│ │  Evals   │   │   │
│  │  │ 模型路由  │ │ 提示词   │ │ HITL流程  │ │ 安全护栏  │ │ 评估系统  │   │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘   │   │
│  └──────────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Data Layer (@juchang/db)                           │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │                    Drizzle ORM + drizzle-typebox                     │    │
│  │              (TypeScript Native, Schema 自动派生 TypeBox)             │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
│                                    │                                         │
│                                    ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────────┐    │
│  │              PostgreSQL + PostGIS + pgvector (Docker)                │    │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐       │    │
│  │  │  users  │ │activities│ │ conver- │ │ partner │ │ notifi- │       │    │
│  │  │         │ │+embedding│ │ sations │ │ intents │ │ cations │       │    │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘ └─────────┘       │    │
│  │                    LBS 地理查询 + 向量语义搜索                         │    │
│  └─────────────────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           AI Provider Layer                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────┐        ┌─────────────────────────┐            │
│  │       Qwen3 (主力)        │        │      DeepSeek (备选)      │            │
│  │  qwen-flash (极速)        │        │  deepseek-chat          │            │
│  │  qwen-plus (深度思考)     │        │  deepseek-reasoner      │            │
│  │  qwen-max (Tool Calling)│        │                         │            │
│  └─────────────────────────┘        └─────────────────────────┘            │
└─────────────────────────────────────────────────────────────────────────────┘
```

| 模块 | 技术栈 | 说明 |
|------|--------|------|
| **小程序** | 微信原生 + TS + LESS + Zustand | 零运行时，极致性能 |
| **Admin 后台** | Vite + React 19 + TanStack Router | Eden Treaty 调用 API |
| **API 网关** | ElysiaJS + Bun | TypeBox Schema 契约驱动 |
| **数据库** | PostgreSQL + PostGIS + pgvector | LBS + 向量语义搜索 |
| **ORM** | Drizzle ORM + drizzle-typebox | TypeScript Native |
| **AI** | Vercel AI SDK + Qwen3 | Agent 架构 + RAG + Rerank |

## 📁 项目结构

```
juchang/
├── apps/
│   ├── miniprogram/      # 微信原生小程序 (Chat-First UI)
│   ├── admin/            # Vite + React 管理后台
│   └── api/              # ElysiaJS API 服务器
│       └── src/modules/
│           ├── auth/     # 微信登录、手机号绑定
│           ├── users/    # 用户管理
│           ├── activities/ # 活动 CRUD
│           ├── chat/     # 活动群聊
│           └── ai/       # AI 模块 (核心)
│               ├── agent/      # Agent 封装层
│               ├── rag/        # RAG 语义检索
│               ├── intent/     # 意图识别
│               ├── memory/     # 记忆系统
│               ├── tools/      # Tool 系统
│               ├── models/     # 模型路由
│               ├── prompts/    # Prompt 工程
│               ├── workflow/   # HITL 工作流
│               ├── guardrails/ # 安全护栏
│               ├── observability/ # 可观测性
│               └── evals/      # 评估系统
├── packages/
│   ├── db/               # Drizzle ORM (10 张核心表)
│   ├── utils/            # 通用工具
│   └── ts-config/        # TypeScript 配置
└── docker/               # PostgreSQL + PostGIS + pgvector
```

## 🚀 快速开始

### 前置要求

- **Bun** >= 1.1.0 ([安装 Bun](https://bun.sh))
- **Docker** (PostgreSQL 数据库)
- **微信开发者工具** (小程序开发)

### 一键启动

```bash
# 克隆并进入项目
git clone <repository-url>
cd juchang

# 一键设置并启动
bun run setup && bun run dev:full
```

### 分步设置

```bash
# 1. 初始化环境变量
bun run env:init

# 2. 安装依赖
bun install

# 3. 启动数据库
bun run docker:up

# 4. 推送 Schema
sleep 5 && bun run db:push

# 5. 启动开发环境
bun run dev:full
```

## 🔧 开发命令

```bash
# 开发服务
bun run dev           # 启动所有服务
bun run dev:api       # 仅启动 API
bun run dev:admin     # 仅启动 Admin
bun run dev:full      # API + 自动 SDK 生成

# 数据库
bun run db:migrate    # 执行迁移
bun run db:generate   # 生成迁移文件
bun run db:studio     # Drizzle Studio

# 代码生成
bun run gen:api       # 生成 Orval SDK (小程序)

# Docker
bun run docker:up     # 启动数据库
bun run docker:down   # 停止数据库
```

## 🔗 服务地址

- **API**: http://localhost:3000
- **OpenAPI**: http://localhost:3000/openapi/json
- **Admin**: http://localhost:5173
- **Drizzle Studio**: `bun run db:studio`

## 📚 文档

- [PRD 产品需求文档](docs/PRD.md) - 完整产品功能描述
- [TAD 技术架构文档](docs/TAD.md) - 完整技术架构设计

## 📝 许可证

MIT License
